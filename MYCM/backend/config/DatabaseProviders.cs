namespace backend.config
{
    /// <summary>
    /// Enum representing the available database providers.
    /// </summary>
    public enum DatabaseProviders
    {
        InMemory = 0,
        MySQL = 1,
        SQLServer = 2,
        SQLite = 3

    }
}